[project]
name = "bigbill"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.95.0",
    "django>=6.0.4",
    "httpx>=0.28.1",
    "pillow>=12.2.0",
]
